<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.f17.csci5539.team5.ceitmajorselectionapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/AppTheme2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainMenuActivity" />
        <activity
            android:name=".RegisterAccountActivity"
            android:theme="@style/AppTheme2" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".MajorsActivity" />
        <activity android:name=".SelectedMajorActivity" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/AppTheme2" />
        <activity android:name=".DiscoveryActivity" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".ResultsActivity" />
        <activity android:name=".ViewResultsActivity"></activity>
    </application>

</manifest>